# Secret used to sign the admin session cookie. Generate with: openssl rand -base64 32
SESSION_SECRET=

# Optional: override where the SQLite database file is written
# DATABASE_PATH=./data/mapple-view.db
